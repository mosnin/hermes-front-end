{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "npm install",
  "buildCommand": "if [ -n \"$CONVEX_DEPLOY_KEY\" ]; then npx convex deploy --cmd 'npm run build'; else echo 'No CONVEX_DEPLOY_KEY — building frontend only (set it to also deploy Convex functions)'; node scripts/gen-generated.mjs && npm run build; fi"
}
